import { createTheme } from '@mui/material/styles';

const theme = createTheme ({
  palette: {
    primary: {
      main: '#ec407a',
      light: '#ef6694',
      dark: '#ec407a'
    }
  }
});

export default theme;